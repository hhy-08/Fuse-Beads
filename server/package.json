{
  "name": "fuse-pdf-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:full": "wrangler deploy -c wrangler.containers.toml",
    "dev:full": "wrangler dev -c wrangler.containers.toml",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.30",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "~5.6.2",
    "wrangler": "^4.26.0"
  }
}
