# env
VITE_ENV = test

# API地址（部署 fuse-pdf-api 后替换）
VITE_API_BASE_URL = https://fuse-pdf-api.example.workers.dev

# 应用标题
VITE_APP_TITLE = Fuse 工具箱-测试

# 根路由（Cloudflare Pages 挂在域名根路径，需为 /）
VITE_BASE_ROUTE = /
